/*
const mainImg = document.querySelector("#main-img");

const scale = 0.05;

document.addEventListener("mousemove", (e) => {
  mainImg.style.backgroundPositionX = -e.screenX * scale + "px";
  mainImg.style.backgroundPositionY = -e.screenY * scale + "px";

  let mox = ((e.screenX - (screen.width * 0.5)) / screen.width);
  let moy = ((e.screenY - (screen.height * 0.5)) / screen.height);
});*/
